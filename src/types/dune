(library
 (name types)
 (public_name unix-sys-socket.types)
 (libraries constants ctypes.stubs))

(rule
 (targets unix_sys_socket_constants.ml)
 (deps    (:gen ../generator/gen_constants_c))
 (action  (system "%{gen} > %{targets}")))
